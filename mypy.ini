[mypy]
python_version = 3.12
ignore_missing_imports = True
# CI type-checks the typed, pure modules (money/config/auth). The router + db layer
# leans on dynamic sqlite row dicts, so it's intentionally not under mypy yet.
files = money.py, config.py, auth.py
