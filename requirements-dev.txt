# Dev / CI tooling (not needed at runtime). Install: pip install -r requirements-dev.txt
# Kept minimal: pip-audit and schemathesis are installed per-job in CI so a tooling
# hiccup there can't break the core test/lint jobs.
-r requirements.txt
ruff==0.6.9
mypy==1.11.2
hypothesis==6.168.0
